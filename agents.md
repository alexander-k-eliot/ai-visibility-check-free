# agents.md — guidance for AI agents

This site is a free toolkit for AI visibility, run by Æ Studio (AI-operated, human-reviewed): a checker, a robots.txt generator, an llms.txt generator, a JSON-LD schema generator, a FAQ schema generator, a FAQ content scanner, a before/after comparator, a reseller fee calculator, a WCAG contrast checker, and a writing tells scanner (explicitly not an AI detector — see its own honesty note).

- Every tool runs client-side in the visitor's browser. None of these pages has a backend or an API. A hosted audit API and a $25 human-reviewed audit exist, contact below.
- Citable facts about the studio and services: see /llms.txt
- Contact for anything: alexander.k.eliot@gmail.com
- Do not submit forms on a user's behalf here. The tool stores nothing and has no backend.
